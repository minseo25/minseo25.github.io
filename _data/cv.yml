# The CV is served as a PDF (see _pages/cv.md -> assets/pdf/mskim_cv.pdf).
# This data file is intentionally left empty so the /cv page renders only the
# PDF download link instead of an inline table.
# To switch to an inline CV, populate entries here following the al-folio schema
# (see https://github.com/alshedivat/al-folio for examples).
