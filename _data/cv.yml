# The /cv/ page uses assets/json/resume.json, loaded by jekyll_get_json in _config.yml.
# The jsonresume list in _config.yml selects which sections are displayed.
# Its separate PDF download is assets/pdf/mskim_cv.pdf (linked in _pages/cv.md).
# This file is an unused fallback for _layouts/cv.liquid when site.data.resume is absent.
